/*
#include<stdio.h>
#include<string.h>

void *ft_memset(void* b, int c, size_t len)
{
    for (int i = 0; i < len; i++) {
        void* b = c;
    }
}

int main(void)
{
    char arr1[] = "blockdmask blog";
    ft_memset(arr1, 'c', 5 * sizeof(char));
    printf(arr1);

    return 0;
}*/
